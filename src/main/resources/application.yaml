server:
  port: 8080
  
spring:
  application:
    name: greeting-service
  config:
    import: optional:configserver:http://localhost:8888 # Usar o configserver remoto
  cloud:
    config:
      profile: it
      
management:
  endpoints:
    web:
      exposure:
        include:
        - '*'
  endpoint:
    health:
      show-details: ALWAYS        

#greeting-service:
#  greeting: "Hello"
#  default-value: "World"
  
logging:
  level:
#     root: WARN
     com.trsystems: INFO